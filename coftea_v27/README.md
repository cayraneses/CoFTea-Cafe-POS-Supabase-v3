# cof/tea Café POS v3 — Supabase Cloud

Use `README-APP.md` for setup.

The application keeps the existing cof/tea POS UI while using Supabase Auth, Postgres and Realtime so a PC, phone and tablet can use the same cloud data from different networks.
